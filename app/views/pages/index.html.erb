<div class="well">
    <h4>Articles</h4>
    <% @articles.each do |a| %>
        <p>
            <strong><%= a.user.username %> </strong>
            <h5><%= link_to a.title, a %></h5>
            <%= truncate(a.body) %></p>
    <% end %>
</div>


